# Copy to deploy.env and fill in. Used by scripts/cloudshell-deploy.sh.
# deploy.env is gitignored so your secrets never get committed.

SUPABASE_URL=https://bacboxpiygqdlytdkeou.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=
# Optional: Cloud Run region (defaults to europe-west1)
# REGION=europe-west1
